#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x = 111, y = 222, z = 333;
    printf("%d + %d + %d = %d\n", x, y, z, (x + y + z));
    printf("%d * %d * %d = %d\n", x, y, z, (x * y * z));
    return 0;
}